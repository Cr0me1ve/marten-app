name: marten_native_resume
description: Android Keystore-backed native resume configuration bridge for Marten.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.4
  flutter: ^3.38.5

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: app.marten.native_resume
        pluginClass: MartenNativeResumePlugin
